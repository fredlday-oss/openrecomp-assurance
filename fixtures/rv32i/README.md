# RV32I Fixtures

Only synthetic, public-domain, or explicitly authorised fixtures belong here.

Initial fixture set should include:

- `equivalent_a` and `equivalent_b`: independently built, distinct binary hashes, same defined semantics;
- five seeded semantic divergences, each expected to be detected by the assurance comparator;
- at least one malformed evidence case expected to fail closed.

Do not commit proprietary game executables or derived sensitive material.
